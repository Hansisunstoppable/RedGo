# RedGo
A high-performance Redis server implemented from scratch. Features include a custom RESP protocol parser over TCP, an in-memory database engine, AOF persistence, and Redis Cluster support for high availability and scalability.
